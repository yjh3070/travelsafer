<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <script type="text/javascript">
        function formTest(){
            if(!document.form.cusId.value || !document.form.cus_pass.value || !document.form.cus_pass_check.value || !document.form.cus_add.value || !document.form.cus_add2.value || !document.form.cus_add3.value || !document.form.cus_phone.value || !document.form.cus_year.value || !document.form.cus_sex.value){
                alert("모든 입력을 해주세요");
                return false;
            }
        }

        function numkeyCheck() { 
            if((event.keyCode >= 48) && (event.keyCode <= 57)) 
                return true; 
            else 
                event.returnValue = false;
        }
    </script>
</head>
<body>
    <form action="/signup" method="post" name="form" onsubmit="return formTest()">
        <input type="text" name="cusId" placeholder="아이디"> <br>
        <input type="password" name="cus_pass" placeholder="비밀번호"> <br>
        <input type="password" name="cus_pass_check" placeholder="비밀번호 확인"> <br>
        <input type="text" name="cus_name" placeholder="이름"> <br>
        <input type="text" name="cus_add" placeholder="주소"> <br>
        <input type="text" name="cus_add2" placeholder="상세주소"> <br>
        <input type="text" name="cus_postalCode" placeholder="우편번호"> <br>
        <input type="tel" name="cus_phone" placeholder="전화번호" maxlength="11" oninput="this.value = this.value.replace(/[^0-9.]/g, '').replace(/(\..*)\./g, '$1');"> <br>
        <input type="text" name="cus_year" placeholder="출생년도"> <br>
        <input type="radio" name="cus_sex" value="1" checked="checked">남
        <input type="radio" name="cus_sex" value="0">여 <br>
        <input type="submit" value="Singup">
    </form>
</body>
</html>