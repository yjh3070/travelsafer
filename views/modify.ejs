<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <script type="text/javascript">
        function formTest(){
            if(!document.form.cus_name.value || !document.form.cus_add.value || !document.form.cus_add2.value || !document.form.cus_postalCode.value || !document.form.cus_phone.value || !document.form.birthday.value || !document.form.cus_sex.value){
                alert("모든 입력을 해주세요");
                return false;
            }
        }

        function numkeyCheck() { 
            if((event.keyCode >= 48) && (event.keyCode <= 57)) 
                return true; 
            else 
                event.returnValue = false;
        }
    </script>
</head>
<body>
    <form action="/modify" method="post" name="form" onsubmit="return formTest()">
        <input type="text" name="cus_name" placeholder="이름" value="<%=results[0].cus_name%>"> <br>
        <input type="text" name="cus_add" placeholder="주소" value="<%=results[0].cus_add%>"> <br>
        <input type="text" name="cus_add2" placeholder="상세주소" value="<%=results[0].cus_add2%>"> <br>
        <input type="text" name="cus_postalCode" placeholder="우편번호" value="<%=results[0].cus_postalCode%>"> <br>
        <input type="tel" name="cus_phone" placeholder="전화번호" maxlength="11" oninput="this.value = this.value.replace(/[^0-9.]/g, '').replace(/(\..*)\./g, '$1');" value="<%=results[0].cus_phone%>"> <br>
        <input type="date" name="birthday" placeholder="생일" value="<%=results[0].cus_birthday%>"><br>
        <%if(results[0].cus_sex == 1){%>
            <input type="radio" name="cus_sex" value="1" checked="checked">남
            <input type="radio" name="cus_sex" value="0">여 <br>
        <%}else{%>
            <input type="radio" name="cus_sex" value="1">남
            <input type="radio" name="cus_sex" value="0" checked="checked">여 <br>
        <%}%>
        <input type="submit" value="회원정보 수정">
    </form>
    <a href="/">돌아가기</a>
</body>
</html>